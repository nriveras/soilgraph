# Encode boundary properties into visual representation

Creates visual properties for boundary rendering based on descriptive
attributes.

## Usage

``` r
create_boundary_encoding(boundary_grade = "clear", boundary_shape = "smooth")
```

## Arguments

- boundary_grade:

  Character: boundary distinctness

- boundary_shape:

  Character: boundary topography

## Value

List with visual properties: linewidth, linetype, smoothing
