# Encode fragment properties into visual encoding scheme

Creates a mapping of fragment properties to visual attributes (size,
transparency, color, shape).

## Usage

``` r
create_fragment_encoding(horizon_data)
```

## Arguments

- horizon_data:

  Data frame with horizon properties

## Value

List with named elements describing visual encoding
