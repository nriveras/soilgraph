# Normalize fragment type

Normalize fragment type

## Usage

``` r
normalize_fragment_type(fragment_type)
```

## Arguments

- fragment_type:

  Character: type descriptor

## Value

Character: normalized type or "gravel" as fallback
